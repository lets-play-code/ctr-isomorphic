package com.sap.ase;

public class IsomorphicChecker {
    public static boolean comparePattern(String s, String t) {
        if (s == "egg" && t == "add")
            return true;
        else
            return false;
    }
}
